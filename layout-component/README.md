# Layout Component

Exploring the **Layout Component** pattern — reusable layout wrappers that accept children or component props.

## Examples

- `SplitScreen` — two-panel layout with `Left` and `Right` component props

## Run

From the repo root:

```bash
npm run dev:layout
```

Or from this folder:

```bash
npm run dev
```
